```bash
                --with-http_image_filter_module \
                --with-http_geoip_module \
                --add-module=/tmp/nginx-http-concat-1.2.2 \


sed -i 's/dl-cdn.alpinelinux.org/mirrors.aliyun.com/g' /etc/apk/repositories

apk add --no-cache --virtual .build-deps gcc make libc-dev openssl-dev pcre-dev zlib-dev
apk add --no-cache openssl pcre zlib perl

wget -O openresty.tgz https://openresty.org/download/openresty-1.21.4.1.tar.gz 
tar -xf openresty.tgz 
cd openresty-1.21.4.1 
wget -O nginx-http-concat-1.2.2.tgz https://ghproxy.com/https://github.com/alibaba/nginx-http-concat/archive/refs/tags/1.2.2.tar.gz 
tar -xf nginx-http-concat-1.2.2.tgz 

./configure --prefix=/usr/local/openresty/nginx \
    --without-mail_pop3_module \
    --without-mail_imap_module \
    --without-mail_smtp_module \
    --with-pcre-jit \
    --with-stream \
    --with-stream_ssl_module \
    --with-stream_ssl_preread_module \
    --with-http_v2_module \
    --with-http_stub_status_module \
    --with-http_realip_module \
    --with-http_addition_module \
    --with-http_auth_request_module \
    --with-http_secure_link_module \
    --with-http_random_index_module \
    --with-http_gzip_static_module \
    --with-http_sub_module \
    --with-http_dav_module \
    --with-http_flv_module \
    --with-http_mp4_module \
    --with-http_gunzip_module \
    --with-threads \
    --with-compat \
    --with-stream \
    --with-http_ssl_module



make
make install
```